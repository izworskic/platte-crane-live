import {NextResponse} from 'next/server'; import {fetchRiverGauges} from '@/lib/sources/usgs'; import {fetchCorridorWeather} from '@/lib/sources/nws'; import {latestVerifiedSurvey} from '@/lib/sources/craneTrust';
export async function GET(){const [river,weather]=await Promise.all([fetchRiverGauges(),fetchCorridorWeather()]); const latest=latestVerifiedSurvey(); return NextResponse.json({status:'ok',mode:'year-round graceful degradation',sources:{craneTrust:{available:!!latest,latestDate:latest?.date},usgs:{available:river.some(x=>x.discharge!=null),gauges:river.map(x=>({site:x.site,freshness:x.freshness}))},nws:{available:weather.some(x=>x.hours.length>0)}},generatedAt:new Date().toISOString()})}
