# CI validation

This file exists to exercise the release-candidate workflow through a pull request. The workflow installs dependencies, runs the deterministic unit suite and model backtest, lints the project, and performs a production Next.js build on Linux/Node 22.
